
public class hashingAnapplicationtoll {
	static int hashtableSize=23;
	static int hashforHashTable(int hashOne) {
		return hashOne%hashtableSize;
	}
	static void intoHashTable() {
		
	}
	static void string2Int(String[] str) {	
		String now="";
		int i,j,strInt=0;
		for(i=0;i<str.length;++i) {
			now+=str[i].length();
		for(j=0;j<now.length();++j)	{
			System.out.println(
					strInt+=1*now.charAt(j)
					);
		}
		}
	}
	/* as here convert for each of the String. so we first have to loop through of the elements
	 * the loops we need for accesssing the strings, and then in the str(S) the characters
	 * */
public static void main(String Args[]) {
	String []str= {" Eric "};
			hashingAnapplicationtoll.string2Int(str);
}
}

